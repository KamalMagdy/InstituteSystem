<%= form_with(model: assignmentstaffstudent, local: true) do |form| %>
  <% if assignmentstaffstudent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assignmentstaffstudent.errors.count, "error") %> prohibited this assignmentstaffstudent from being saved:</h2>

      <ul>
      <% assignmentstaffstudent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
  <%= form.hidden_field :student_id, :value => @studentid %>
  </div>
  <div class="field">
  <%= form.hidden_field :admin_user_id, :value => @staffid %>
  </div>
  <div class="field">
  <%= form.hidden_field :assignment_id, :value => @assignmentid %>
  </div>
  <div class="field">
  <%= form.hidden_field :course_id, :value => @courseid %>
  </div>

  <%= form.label :file %>   
  <%= form.file_field :file %>  
  <div class="field">
  <%= form.label :derlivered_assignment %>   
  <%= form.text_field :derlivered_assignment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>